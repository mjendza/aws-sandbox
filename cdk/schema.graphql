type User {
  id: String!
  email: String!
}

type PaginatedUsers {
  items: [User]
  nextToken: String
}

type Query {
  getUserById(id: String!): User
  listUsers(limit: Int, nextToken: String): PaginatedUsers
}

type Mutation {
  syncUsers: [User]
}